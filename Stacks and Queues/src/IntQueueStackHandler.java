/**
 * Interface with a single (abstract) method, that each menu handler class should implement - so that menu handling is generic in "Manager" class.
 * @author AlexeyRaskin
 */
public interface  IntQueueStackHandler {
	public void processRequest();
}